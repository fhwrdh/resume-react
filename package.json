{
  "name": "resume-react",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "mdi-react": "^3.3.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-responsive": "^9.0.2",
    "react-scripts": "5.0.1",
    "styled-components": "^5.3.11"
  },
  "scripts": {
    "deploy": "npm run build && cp -r build/* ~/webapps/fhwrdh_resume_react",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "publish": "./publish.sh"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
